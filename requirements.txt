# Reproducible runtime pin set for Docker builds and CI.
# This file is a frozen lockfile; pyproject.toml is the source of truth for
# version floors. If you change a dep, update pyproject.toml first, then
# regenerate this file via:
#   pip-compile --output-file=requirements.txt pyproject.toml
# (or manually keep both in sync until pip-compile is wired into CI).

typer==0.12.3
rich==13.7.1
pydantic==2.7.4
pydantic-settings==2.3.4
oracledb==2.2.1
numpy==1.26.4
igraph==0.11.5
jinja2==3.1.4
httpx==0.27.0
oci==2.126.4
pyyaml==6.0.1
tiktoken==0.7.0
pypdf==4.2.0
markdown-it-py==3.0.0
tree-sitter==0.24.0
tree-sitter-language-pack==0.5.0
tenacity==8.3.0
