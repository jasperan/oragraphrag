"""LightRAG baseline — stub until Task 16."""

from oragraphrag.config import Config


async def run(question: str, cfg: Config) -> dict:
    raise NotImplementedError(
        "LightRAG baseline is wired in Task 16. Install `lightrag-hku` and "
        "configure it to use the same OCI/Ollama LLM as the rest of the suite."
    )
